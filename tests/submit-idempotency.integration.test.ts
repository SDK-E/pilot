import assert from "node:assert/strict";
import test from "node:test";

test("BLOCKED: AC-08-03 — double click and retry HTTP produce one user message and one execution", () => {
  assert.fail(
    "BLOCKED: requires database with conversation state and idempotency tables.",
  );
});
