/**
 * Names the pain the product exists to fix before the feature list makes
 * its case. Kept to one short beat — this is a transition, not a section.
 */
export function ProblemStrip() {
  return (
    <section className="mx-auto max-w-6xl px-4 sm:px-6">
      <div className="border-t py-8">
        <p className="max-w-2xl text-sm leading-relaxed text-muted-foreground sm:text-base">
          Most AI gives you an answer and calls it done. You&apos;re still the
          one turning it into a plan, a finished task, or a real code change.
        </p>
      </div>
    </section>
  );
}
